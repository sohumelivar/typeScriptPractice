name: Project test & build
on:
  push:
    branches: [ myDev ]
  pull_request:
    branches: [ myDev ]

jobs:
  build-and-deploy:
    runs-on: macos-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
        with:
          persist-credentials: false

      - name: Install 🔧
        run: npm install

      - name: Run Test 🔨
        run: npm run lint:ts